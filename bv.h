/*check if bit is 1 */
int  bit_test(const int);

/* raise bit to 1 */
void bv_on(const int);

/* turn of a bit */
void bv_off(const int);

/* return index of a free bit */
int bv_index();

/* initialize the bit vector */
void bv_init();
